#include <iostream>
using namespace std;

long long int fact(int num)
{ if(num==1)
    return num;

 return num * fact(num-1);


}

int main() {
	int t;
cin>>t;
while(t>0)
{
    int N,M;
cin>>N>>M;
cout<<fact(N+M)/(fact(N)*fact(M));



t--;

}
	return 0;
}
